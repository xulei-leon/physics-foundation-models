# Sprint M1-02 Document Review

Reviewed file: `D:\code\particleML\project\3-Plan\sprint-m1-02.md`

Governing context:

- `D:\code\particleML\project\1-Requirement\FR-001-reference-demo-diagnostics.md`
- `D:\code\particleML\project\3-Plan\2026-07-30-reference-demo-diagnostics-plan.md`
- `D:\code\particleML\project\3-Plan\sprint-m1-01.md`
- `D:\code\particleML\project\3-Plan\sprint-m1-03.md`
- `D:\code\particleML\docs\software\requirements.md`
- `D:\code\particleML\docs\software\architecture.md`
- `D:\code\particleML\docs\software\specification.md`
- `D:\code\particleML\schemas\study-result.schema.json`
- Repository verification gates described by the FR and source adaptation plan.

## Findings

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness / Scientific-policy compliance | `project/3-Plan/sprint-m1-02.md:87` to `project/3-Plan/sprint-m1-02.md:90`; `docs/software/specification.md:44` to `docs/software/specification.md:56` | Nominal-simulation filtering is underspecified for the planned helper. The sprint says the helper uses only `target`, `raw_score`, `split`, and `w_yield`, then separately requires excluding data and generator variations. Those four arrays are not sufficient to prove or implement that exclusion unless upstream filtering is made an explicit contract. | The prediction payload contract retains `is_data`, `sample_role`, `sample_partition`, and `variation_of`, and explicitly says this metadata lets nominal templates exclude generator variations. FR-001 also requires data and generator variations to be excluded from the weighted-KS diagnostic. | Make the filtering boundary explicit. Either require the helper to accept `is_data` and nominal-sample metadata, or require `study.py` to pre-filter a prediction frame to `is_data == false` and `sample_role == "nominal"` before passing arrays. Add a focused test containing data and generator-variation rows with extreme `raw_score` values and assert they do not affect either class KS value. |
| High | Test / Verification | `project/3-Plan/sprint-m1-02.md:59` to `project/3-Plan/sprint-m1-02.md:63`; `project/3-Plan/sprint-m1-02.md:92` to `project/3-Plan/sprint-m1-02.md:96`; `project/3-Plan/sprint-m1-02.md:123` to `project/3-Plan/sprint-m1-02.md:134`; `project/3-Plan/sprint-m1-03.md:51` to `project/3-Plan/sprint-m1-03.md:54` | M1-02 touches `study.py` and requires every seed and ensemble run to carry the new record, but its executable focused verification runs only `tests/test_evaluation.py` and explicitly defers `tests/test_demo.py` to M1-03. That leaves the M1-02 study integration path under-specified unless `tests/test_evaluation.py` is expanded into a synthetic study-record integration test. | FR-001 requires records for all four models, five seeds, and ensembles. The source adaptation plan requires a test proving the fixed study writes diagnostics for five seeds and the ensemble for all four models. M1-03 will verify the full Demo later, but M1-02 acceptance already claims seed and ensemble run records accept the optional schema field. | Add a lightweight focused integration test in M1-02 that is executable without the full Demo. It should construct or exercise the study run-record path with synthetic predictions, then assert `cut_based`, `logistic`, `xgboost`, and `mlp` each contain `seed-17`, `seed-42`, `seed-314`, `seed-2026`, `seed-2718`, and `ensemble` diagnostics. If this needs a small record-building helper in `study.py`, include that helper in the sprint scope and keep the full offline Demo assertion in M1-03. |
| High | Contract / Schema compatibility | `project/3-Plan/sprint-m1-02.md:95` to `project/3-Plan/sprint-m1-02.md:96`; `project/3-Plan/sprint-m1-02.md:114`; `schemas/study-result.schema.json:27` to `schemas/study-result.schema.json:31`; `project/1-Requirement/FR-001-reference-demo-diagnostics.md:110` to `project/1-Requirement/FR-001-reference-demo-diagnostics.md:112` | The sprint says to extend the optional study-result property, but the current study-result schema does not validate model run contents. As written, `models` only requires the four model keys and allows each value to be any object, so simply adding an optional field may not validate the diagnostic shape, constants, numeric range, or forbidden threshold/pass/blocking fields. | The existing schema has `"models": {"type": "object", "required": ["cut_based", "logistic", "xgboost", "mlp"], "additionalProperties": {"type": "object"}}`. FR-001 requires extending the schema enough to validate `raw_score_shape_diagnostics` while keeping contract version `2.1.0`. | Require explicit schema coverage for the nested diagnostic object where it appears in run records. At minimum, validate `comparison == "train-vs-test"`, `weighting == "absolute-w_yield"`, each KS value as `null` or a number in `[0,1]`, and no extra fields inside the diagnostic object. Add positive and negative schema tests proving malformed diagnostics and threshold/pass/blocking additions fail. |
| Medium | Test / Weighted empirical-CDF semantics | `project/3-Plan/sprint-m1-02.md:82` to `project/3-Plan/sprint-m1-02.md:85`; `project/3-Plan/sprint-m1-02.md:101` to `project/3-Plan/sprint-m1-02.md:103`; `project/3-Plan/2026-07-30-reference-demo-diagnostics-plan.md:143` to `project/3-Plan/2026-07-30-reference-demo-diagnostics-plan.md:150`; `docs/software/specification.md:73` to `docs/software/specification.md:74` | The sprint requires stable sorting and right-continuous empirical CDFs, but the listed tests do not include a repeated-score/tie case that distinguishes the KS convention from DDT midranks, interpolation, or left-continuous handling. Identical/disjoint/negative-weight cases can pass while the tie convention is wrong. | The source adaptation plan explicitly says not to reuse the DDT helper because DDT uses weighted midranks, while weighted KS requires comparable right-continuous CDFs. The software specification documents DDT midranks separately, increasing the risk of accidental reuse. | Add a deterministic tied-score test with repeated scores and unequal or asymmetric weights, with an exact expected KS value under right-continuous CDF semantics. The test should fail for midrank-based or interpolated implementations. |
| Medium | Null behavior / Contract clarity | `project/3-Plan/sprint-m1-02.md:82` to `project/3-Plan/sprint-m1-02.md:91`; `project/3-Plan/sprint-m1-02.md:101` to `project/3-Plan/sprint-m1-02.md:107`; `project/1-Requirement/FR-001-reference-demo-diagnostics.md:164` to `project/1-Requirement/FR-001-reference-demo-diagnostics.md:167` | The sprint mentions an `empty-positive-weight` test and separately says a class returns `null` only when train or test lacks positive absolute weight, but it does not state whether the primitive rejects zero-total samples and the higher-level diagnostic helper converts that condition to class-specific `null`. This ambiguity can produce too-broad `null` behavior or a primitive that silently treats an invalid distribution as valid. | FR-001 separates failure modes: malformed, misaligned, or non-finite weighted-KS inputs fail closed with `ContractError`; missing positive absolute weight for one required class/split produces `null` only for that class diagnostic. | Split the tests and implementation contract explicitly: `weighted_ks_distance` should reject samples without positive total absolute weight, while the class-conditional diagnostic helper should return `null` only for the affected `signal_weighted_ks` or `background_weighted_ks` field and still compute the other class when valid. |
| Low | Planning freshness | `project/3-Plan/sprint-m1-02.md:16`; `project/3-Plan/sprint-m1-02.md:157`; `project/3-Plan/sprint-m1-01.md:15`; `project/3-Plan/sprint-m1-01.md:166` to `project/3-Plan/sprint-m1-01.md:168` | The sprint still says it starts after M1-01 and has a delivery conclusion pending M1-01 completion, but M1-01 is already marked complete with focused verification passed. This is not a scientific defect, but it can create stale workflow evidence when M1-02 starts. | M1-01 status is complete and says no open M1-01 blocker remains. M1-02 still says `Planned - starts after M1-01 completes` and `Pending M1-01 completion`. | Update the status/prerequisite wording before execution to state that the M1-01 prerequisite has been satisfied, while preserving that M1-02 implementation and focused verification are still pending. |

## Checks With No Actionable Finding

- No instruction in M1-02 relaxes blinding, freeze gates, DDT gates, expected-fit behavior, or the primary XGBoost-minus-cut-based comparison.
- The sprint correctly keeps the diagnostic non-blocking and excludes thresholds, `passed`, blocking fields, retuning, new dependencies, and reference-project imports.
- The sprint correctly defers the long-running full Demo and complete repository regression to M1-03, provided M1-02 adds a lightweight focused integration test for its own study-record changes.
- M1-01 completion evidence is present and supports starting M1-02 after the planning freshness update above.
